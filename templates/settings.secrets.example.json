{
  "$comment": "个人密钥层示例（复制为 .claude/settings.<name>.secrets.json，gitignored 永不提交）：只放 token，结构由 tracked settings.<name>.json 提供，project:sync 渲染时合并。",
  "env": {
    "ANTHROPIC_AUTH_TOKEN": "sk-your-personal-token"
  }
}
