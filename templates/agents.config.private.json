{
  "$comment": "私人单机档：结构+密钥同层（快照可直接 tracked 于私仓）。把占位符替换为实际值；不需要的键删除即可。",
  "mcp": {
    "defaultProfile": "private",
    "keys": { "privateToken": "private" },
    "requiredKeys": { "private": ["privateToken"] },
    "slotUrlMarkers": [],
    "gatewayBase": "https://your-gateway.example.com",
    "profiles": {
      "private": {
        "example-mcp": { "type": "http", "url": "${gatewayBase}/mcp/example", "token": "${privateToken}" }
      }
    }
  },
  "defaultSubscription": "private",
  "worktree": {
    "dirPrefix": {},
    "residentSlots": [],
    "collectPaths": [".claude"],
    "collectSuffix": ".json",
    "mergeHints": [],
    "mainBranch": "master"
  }
}
